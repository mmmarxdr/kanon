# Overlay: run prebuilt GHCR images instead of building locally.
#
#   docker compose -f docker-compose.yml -f docker-compose.images.yml pull
#   docker compose -f docker-compose.yml -f docker-compose.images.yml up -d
#
# This is what the CD on the dev box uses (no build → no OOM on t4g.small).
# Pin a version by setting KANON_IMAGE_TAG (default: latest) in .env; override
# the registry/org with KANON_IMAGE_PREFIX if you forked.
# Requires Docker Compose v2.24+ (for `!reset`).

services:
  kanon-api:
    build: !reset null
    image: ${KANON_IMAGE_PREFIX:-ghcr.io/mmmarxdr}/kanon-api:${KANON_IMAGE_TAG:-latest}

  kanon-web:
    build: !reset null
    image: ${KANON_IMAGE_PREFIX:-ghcr.io/mmmarxdr}/kanon-web:${KANON_IMAGE_TAG:-latest}
